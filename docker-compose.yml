version: '3'
services:
  app:
    build: ./app
  proxy:
      build: ./web
      ports:
         - "80:80"
  mongo:
      image: "mongo:3.4-xenial"
      volumes:
          - ./mongo/data:/data/db
      restart: always
